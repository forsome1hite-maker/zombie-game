# game

웹(HTML/JS) 기반 게임 프로젝트.

## 실행 방법

`index.html`을 브라우저로 열면 됩니다. 로컬 서버가 필요할 경우:

```bash
# Python이 설치돼 있으면
python -m http.server 8000
# 브라우저에서 http://localhost:8000 접속
```

## 구조

- `index.html` — 진입점
- `style.css` — 스타일
- `main.js` — 게임 로직
